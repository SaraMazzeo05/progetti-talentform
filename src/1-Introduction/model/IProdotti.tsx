export interface IProdotti{
            id? : number,
            brand? : string,
            prezzo?: number,
            colore?: string,
            composizione?: string,
            peso?: string
}